{
  "name": "portfolios",
  "version": "1.0.0",
  "description": "Portfolio artistique avec drag & drop et enrichissement Gemini AI",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:26858/git/salamichel/Portfolios"
  },
  "keywords": ["portfolio", "gallery", "images", "gemini", "ai"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
